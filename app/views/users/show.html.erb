<% provide(:title, @user.name) %>
<% provide(:position, "Profil") %>
<% provide(:column_type, "rating") %>

<% content_for :info do %>
  <%= render 'user_show_info' %>
<% end %>

<% content_for :content do %>
  <%= render 'user_show_ratings' %>
<% end %>
