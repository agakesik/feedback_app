<div class="alert-container">
  <% flash.each do |mssg_type, message| %>
    <%= content_tag :div, class: "alert alert-#{mssg_type}" do %>
      <span class="close" onclick="this.parentElement.style.display='none';">&times;</span>
      <%= message %>
    <% end %>
  <% end %>
</div>
