<% provide(:title, "Skillsy") %>
<% provide(:position, "Skills") %>

<% @skills_categories.each do |category| %>
  <p><strong><%= category.name %></strong></p>
  <ul class="skills">
    <% category.skills.each do |skill| %>
      <li> <%= link_to skill.name, skill_path(skill) %></li>
    <% end %>
  </uL>
<% end %>
