<h2>Oceny</h2>

<% SkaterStatus.all.each do |status| %>
  <% status.users.each do |user| %>
    <div class="row">
      <%= user_cell(user) %>
      <%= rating_cell(user, @skill, true) %>
    </div>
  <% end %>
<% end %>
